/** Maximum number of fix retries before giving up. */
export const MAX_RETRIES = 2;

/** Allowed source directories for patching. */
export const ALLOWED_PATHS = ['src/'];

/** Maximum diff size in characters. */
export const MAX_DIFF_SIZE = 50_000;
